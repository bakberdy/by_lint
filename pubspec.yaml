name: by_lint
description: Custom lint rules for by_ Flutter projects.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: '>=6.0.0 <7.0.0'
  custom_lint_builder: ^0.7.0
